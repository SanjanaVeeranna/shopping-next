/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "picsum.photos",
            "dhunkifashion.com",
            "encrypted-tbn0.gstatic.com"
        ]
    }

};

export default nextConfig;
